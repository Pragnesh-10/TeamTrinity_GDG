services:
  - type: web
    name: sportshield-api
    env: docker
    dockerContext: ./backend
    dockerfilePath: ./backend/Dockerfile
    envVars:
      - key: API_BASE_URL
        value: https://sportshield-api.onrender.com # Change if you rename the service or attach a custom domain
      - key: FRONTEND_URL
        value: https://your-vercel-app-url.vercel.app # Update this with your actual Vercel URL
      - key: PORT
        value: "8000"
